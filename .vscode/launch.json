{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/Notes-App/js/package.json",
                "localRoot": "${workspaceFolder}/Notes-App/js"
            }
        }
    ]
}